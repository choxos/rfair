% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fair_recommendations.R
\name{fair_recommendations}
\alias{fair_recommendations}
\title{Recommend how to fix the failed tests of a FAIR assessment}
\usage{
fair_recommendations(x)
}
\arguments{
\item{x}{A \link{fair_assessment} from \code{\link[=assess_fair]{assess_fair()}}.}
}
\value{
A data frame, most valuable fixes first, with columns
\code{metric_identifier}, \code{test_identifier}, \code{test_name}, \code{points} (the score
passing the test would add, given the metric's cap), and \code{recommendation}.
}
\description{
Lists every metric test that did not pass, with one concrete action a data
curator or software maintainer can take to pass it (for example "Add a
license to the metadata as a URL or SPDX identifier"). Actions come from a
curated table covering the FsF data metrics and the FRSM software metrics;
tests of legacy metric versions fall back to a metric-level action.
}
\examples{
data(fair_example)
head(fair_recommendations(fair_example))
}
\seealso{
\code{\link[=fair_compare]{fair_compare()}} to check the effect of a fix.
}
