% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frsm_agreement.R
\name{frsm_agreement}
\alias{frsm_agreement}
\title{Agreement between rfair's FRSM scores and expert ratings}
\usage{
frsm_agreement(assessments, ratings)
}
\arguments{
\item{assessments}{A list of \link{fair_assessment} objects scored with an FRSM
metric version (for example the \code{"assessments"} attribute of
\code{assess_fair_batch(..., metric_version = "0.7_software", keep = TRUE)}),
or a single assessment.}

\item{ratings}{A data frame with columns \code{identifier} (as passed to
\code{\link[=assess_fair]{assess_fair()}}), \code{test_identifier} (for example \code{"FRSM-14-R1-2"}), \code{rater},
and \code{passed} (logical; did the software satisfy the test?).}
}
\value{
A data frame with one row per test: \code{test_identifier}, \code{n} (rated
items), \code{rfair_pass} and \code{rater_pass} (pass rates), \code{agreement} (share of
items where rfair matches the raters' majority), \code{kappa} (Cohen's kappa of
rfair against the majority), and \code{rater_kappa} (kappa between the first two
raters, when two or more rated the test). Ties between raters are dropped.
}
\description{
rfair scores the FRSM software metrics from repository signals (file names,
API fields, metadata files). These heuristics have not yet been validated
against expert judgement, so every FRSM result carries
\code{evidence_type = "heuristic"}. \code{frsm_agreement()} supports that validation:
raters judge each FRSM test for a sample of repositories (start from the
template in \code{system.file("extdata", "frsm_validation_template.csv", package = "rfair")}), and the function compares rfair's pass/fail result with
the raters' majority judgement, test by test.
}
\examples{
data(fair_example)
ratings <- data.frame(identifier = fair_example$id,
                      test_identifier = "FsF-F1-01MD-1", rater = "A", passed = TRUE)
# fair_example is a data assessment, so this only shows the shape of the output
frsm_agreement(fair_example, ratings)
}
