% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fair_compare.R
\name{fair_compare}
\alias{fair_compare}
\title{Compare two FAIR assessments}
\usage{
fair_compare(a, b, level = c("metric", "test"))
}
\arguments{
\item{a, b}{\link{fair_assessment} objects; \code{a} is the baseline.}

\item{level}{\code{"metric"} (default) for one row per metric, or \code{"test"} for
one row per metric test.}
}
\value{
A data frame with the identifiers, \code{earned_a}, \code{earned_b},
\code{status_a}, \code{status_b}, \code{delta} (\code{earned_b - earned_a}), and \code{change}
(\code{"improved"}, \code{"worse"}, \code{"same"}, or \code{"only in a"}/\code{"only in b"}), in
the metric order of \code{a}.
}
\description{
Joins two assessments, for example of the same record before and after a
metadata fix, or under two metric versions, and reports what changed.
}
\examples{
data(fair_example)
fixed <- fair_example
fixed$results[[1]]$score$earned <- fixed$results[[1]]$score$total
fair_compare(fair_example, fixed)[1:3, ]
}
\seealso{
\code{\link[=fair_recommendations]{fair_recommendations()}}
}
